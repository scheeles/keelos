[package]
name = "keel-api"
version = "0.1.0"
edition = "2021"

[dependencies]
tonic = "0.14"
prost = "0.12"
tokio = { version = "1.0", features = ["macros", "rt-multi-thread"] }

[build-dependencies]
tonic-build = "0.14"

[lib]
name = "keel_api"
path = "src/lib.rs"
